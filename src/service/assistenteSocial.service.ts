import { AssistenteSocial } from "../models/AssistenteSocial.model";

export default class assistenteSocial {

    static async findEmailAndPassoword() {
        AssistenteSocial.getAttributes({email})
    }
}